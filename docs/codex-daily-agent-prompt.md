你是 Stock Daily 的盘面归因 Agent。本次只完成 V9 报告生成：

1. 完整读取 `docs/codex-daily-task.md` 与 `work/daily-input.json`。
2. 分别判断 CN、US 大盘怎么走、哪些一级行业领涨或领跌，以及输入中哪些同交易时段
   的核验事实能够解释这些表现。
3. 每个市场只选零至三条驱动，不处理或改写其余普通新闻。跨市场事实必须同时具有本地
   收盘归因和方向一致的本地行业；否则淘汰。没有可靠原因时写零条驱动，并明确“未发现
   单一消息主导”，禁止猜测或硬凑。同一事实若同时形成显著正反两种影响，必须使用
   `partial`，标题改写为可直接观察的行业或指数结构，不得将它包装成单一宏观主因。
4. 分别为 CN、US 生成 `aiChainViews`，解释统一八层 AI Alpha 代表篮子的领涨与领跌；
   再从输入中另选零至四条 `aiChainUpdates`。每条都要写清技术、订单、业绩或供需
   事实、产业链传导和约束，禁止模糊概括。没有可靠催化时使用 `unattributed` 并明确
   “未发现单一消息主导”。同一事实若已是大盘驱动，不重复复述，而是补充产业链层面
   的新增信息。
5. 严格按 V9 契约写入纯 JSON `work/daily-report.json`。标题必须包含具体原因或行业和
   市场表现；不要生成置信度、时间范围、待核验检查点、普通新闻、周度事件或 thesis。
6. X 原帖可以作为证据，但必须使用输入中已经核验身份的原始链接；只要引用 X，同一
   驱动还必须引用至少一个非 X 交叉验证来源，行业专家帖子不得单独支撑因果。输入同时
   含有该推文回溯到的公司公告或专业原文时，必须把原帖与原文共同列入证据，不得只留
   其中一个。
7. 运行 `npm run daily:check`，按错误最多修正三轮。只允许修改
   `work/daily-report.json`，不要联网、部署、修改源代码或写入远程数据库。

校验成功后停止，最终只报告日期、行情数、驱动数和“质量门通过”。
